import React from "react";
import TodoUserListPage from "../features/todo/TodoUserListPage";

interface Props {}

const Page = ({}: Props) => {
  return <TodoUserListPage />;
};

export default Page;
